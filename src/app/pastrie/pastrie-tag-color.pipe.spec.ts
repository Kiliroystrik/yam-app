import { PastrieTagColorPipe } from './pastrie-tag-color.pipe';

describe('PastrieTagColorPipe', () => {
  it('create an instance', () => {
    const pipe = new PastrieTagColorPipe();
    expect(pipe).toBeTruthy();
  });
});
