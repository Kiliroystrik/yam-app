export class Pastrie {

    public id!: string;
    public ref!: string;
    public name!: string;
    public description!: string;
    public quantity!: number;
    public order!: number;
    public url?: string;
    public tags?: string[];
    public like?: string;

    // public constructor(id: string, ref: string, name: string, description: string, quantity: number, order: number, url?: string, tags?: [], like?: string) {

    // }
}
